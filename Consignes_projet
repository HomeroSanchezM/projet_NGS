# résume, abstract, Jupyter ???
# valeur ajoutée faire un Jupyter.
# ajouter une couche de bash qui appel le script python
# Fichier d'entrée , fichier de mapping: lorsqu'on va générer nos données de séquencages, que j'ai bien
# le nombre de colonnes.

# On va obtenir en sortie des reads(lectures) qui sont des fragments de séquences, dont notre suite de base est connue.
# Une fois obtenues ont fait du mapping sur un génome de référence, pour savoir où elles se situent.
# On va obtenir un fichier de mapping qui va nous dire pour chaque read où il se situe sur le génome de référence.
# Ensuite on va essayer d'identifier les différences et les variations qu'il y'a entre le génome de référence
# et le génome de l'individu séquencé. Imaginons un individu A et un pool d'individus de référence B, C, D.
# Dans le cadre du projet on sur le moodle un fichier .sam qui est un fichier de mapping.

# Le fichier existe, n'est pas vide, pas un répertoire, pas corrompu. bash ?
#Stocker la colonne numéro 2,
# que l'on prend que le corps de fichier et pas l'entete,

# Combien de reads sont mappés ?
# Compter le nombre de reads en fonction du flag de mapping.
# Ou les reads sont mappés ? L'alignement est il homogene selon le chromosome (densité ?) si plusieurs chromosmes,
# notion de densité de représentation des reads selon les chromosomes.
# Ensuite un aspect qualité est ce que les reads de qualités sont mappés : essayer de représenter cela par un graphique.
# On a pas le droit d'utiliser la librairie pysam.


# Créer un dictionnaire : clé (liste de flag dans le cas ou le flag est le meme pour plusieurs reads? de liste ?)
# et informations.

# Exemple input avec les flags 99,0,0,0, 128 ....
# En sortie je veux savoir combien de ligne ont le flag 99, puis O, puis 128 ...
# flag complémentaire, combien de reads mappés ?

IDEES EN VRAC :
# TOOLBAR !!!!
# JUPYTER
# Propose moi des idées pour rendre innovant ce petit programme:
# Un graphique pour représenter la densité de mapping des reads sur les chromosomes.
# Un graphique pour représenter la qualité des reads mappés.
# Un graphique pour représenter la qualité des reads mappés en fonction de leur position sur le génome.
# Faire un script bash qui appelle le script python.
